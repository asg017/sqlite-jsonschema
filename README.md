- [ ] `jsonschema_valid(schema, instance)`
- [ ] or `jsonschema_matches(schema, instance)` ?
- [ ] `jsonschema_schema_valid(schema)`
- [ ] `select * from jsonschema_errors(schema, instance)`

- [ ] drafts?
